(library (syntax (6))
  (export (import (only (rnrs) define-syntax syntax let-syntax letrec-syntax
                        syntax-rules syntax-case free-identifier=?
                        bound-identifier=?)))
  (import (rnrs)))
